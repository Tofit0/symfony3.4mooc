{# src/OC/PlatformBundle/Resources/views/Advert/menu.html.twig #}

{# Ce template n'hérite de personne,
   tout comme le template inclus avec {{ include() }}. #}

<ul class="nav nav-pills nav-stacked">
    {% for applications in listApplications %}
        <li>
            <a href="{{ path('oc_advert_view', {'id': applications.advert.id}) }}">
                {{ applications.advert.title }}
            </a>
        </li>
    {% endfor %}
</ul>